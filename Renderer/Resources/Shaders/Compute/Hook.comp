//-------------------------------------------------------------------------------------------------
//-------------------------------------------------------------------------------------------------
vec4 f_hook(inout Hit hit_data, inout State state, inout Ray ray) {
	// Error
	if (hit_data.material == MAX_UINT) {
		return vec4(1, 0, 1, 1);
	}//---INSERT-SHADERS---//

	return vec4(1, 0, 1, 1);
}